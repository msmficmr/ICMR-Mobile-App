# Project rules

## Testing

- Do not write tests, and do not run the app or tests to verify changes. The user tests everything themselves.
- Static checks are fine (`flutter analyze` on the files you touched) — just don't build, run, or add test files.
